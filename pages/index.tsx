import React from "react";
import Dashboard from "./dashboard";

// this should be sign in field but now use Dashboard Component
// if user isAuth redirect to Dashboard
export default function App() {
  return <Dashboard />;
}
